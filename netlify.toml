[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Fix SPA routing (VERY IMPORTANT for React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
